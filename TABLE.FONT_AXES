CREATE TABLE "FONT_AXES" 
   (	"FONT_ID" NUMBER, 
	"AXIS" VARCHAR2(4), 
	"AXIS_START" NUMBER, 
	"AXIS_END" NUMBER, 
	 CONSTRAINT "PK_FONT_CUSTOM_AXES" PRIMARY KEY ("FONT_ID", "AXIS")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_FONT_CUSTOM_AXES" ON "FONT_AXES" ("FONT_ID", "AXIS") 
  ;