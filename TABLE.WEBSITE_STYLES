CREATE TABLE "WEBSITE_STYLES" 
   (	"WEBSITE_ID" NUMBER NOT NULL ENABLE, 
	"STYLE_ID" NUMBER, 
	"VALUE" VARCHAR2(30), 
	"UPDATED_DATE" TIMESTAMP (6) WITH TIME ZONE DEFAULT current_timestamp, 
	 CONSTRAINT "PK_WEBSITE_STYLES" PRIMARY KEY ("WEBSITE_ID", "STYLE_ID")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_WEBSITE_STYLES" ON "WEBSITE_STYLES" ("WEBSITE_ID", "STYLE_ID") 
  ;

  ALTER TABLE "WEBSITE_STYLES" MODIFY ("WEBSITE_ID" NOT NULL ENABLE);