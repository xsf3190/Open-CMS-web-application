CREATE TABLE "LANGUAGE" 
   (	"CODE" VARCHAR2(2), 
	"UNICODE_RANGE" CLOB, 
	"NAME" VARCHAR2(20), 
	"TEXT" VARCHAR2(3900), 
	 CONSTRAINT "PK_LANGUAGE" PRIMARY KEY ("CODE")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_LANGUAGE" ON "LANGUAGE" ("CODE") 
  ;