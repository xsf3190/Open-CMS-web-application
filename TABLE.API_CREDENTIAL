CREATE TABLE "API_CREDENTIAL" 
   (	"NAME" VARCHAR2(20), 
	"URL" VARCHAR2(50), 
	"KEY" VARCHAR2(107), 
	"SECRET" VARCHAR2(50), 
	 CONSTRAINT "PK_API_CREDENTIAL" PRIMARY KEY ("NAME")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_API_CREDENTIAL" ON "API_CREDENTIAL" ("NAME") 
  ;