CREATE TABLE "ES_MODULES_IMPORTMAP" 
   (	"VERSION" VARCHAR2(4), 
	"DEPLOY_ENV" VARCHAR2(4), 
	"CONTENT" CLOB, 
	"UPDATED_DATE" TIMESTAMP (6) WITH TIME ZONE DEFAULT current_timestamp, 
	"SHA256" VARCHAR2(51)
   ) ;